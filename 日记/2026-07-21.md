1. tmdb的分类是区分movie和tv的，浏览器返回items里面需要区分（之前是混合的）
2. 浏览器返回的sortOption可能需要更换，数据库存了popularity、vote_average、vote_count（好像没用），可以考虑分成受欢迎度（使用popularity降序）、评分（使用vote_average降序，考虑加一个vote_count的最低阈值）、最新（使用air_date降序排列）




- [x] 排查星座控制台接口报错
包错误导入