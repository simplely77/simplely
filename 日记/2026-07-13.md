1. search接口,获取对应影视的元数据和下载状态
>根据media_type,status筛选，finished,待测试
2. get by tmdb接口
>
3. tmdb list接口，list落库定时扫描更新
>
4. 加载单个影片接口，内部调用
>
5.  获取已入库影视信息接口
>
6. 通过已入库资源的mediaid+episode查询种子
>直接接pt站
7. 更新已入库影视种子，同步更新资源库
>更新source库，清理redis，更新状态，加入队列，finished,待测试


先做以入库资源相关接口，比如根据分页获取元数据等等，根据下载状态分组等等


展示相关接口后续再做