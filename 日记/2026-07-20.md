tmdb的api问题，popular不是其对应的页面，更换discover接口，加上过滤器with_type=4，过滤掉talk show级别的tv，只保留常规剧。同时movie接口也改用discover
这周工作：
- [ ] search接口改造，分类改造等等
- [ ] 资源网站details接口，鉴权问题等等

下午查看影片相关的旧逻辑，大致了解现有逻辑，首先需要更新milisearch的全量同步，切换到tmdb_media表，然后是把catagory里的相关的表都切换到自建的表，然后是返回给浏览器的分类标签要更新，然后是筛选逻辑要更新
再就是资源网站details的设计后续还需要进行