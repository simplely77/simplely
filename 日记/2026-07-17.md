# ingest改造
## 1. 职责混乱

还是使用nats自带的重试，自定义设置超时时间，减轻reconciler职责
## 2. 生产者速率远远大于消费者导致的消费者消息堆积，丢失

增加搜索队列自动背压机制，更换订阅顺序来uploading>downloading>searching,因为先uploading之后会释放空间，便于增加downloading处理速度，槽位为8，4，2
## 3.reconciler的职责
首先一点要先改，manager启动时清理qbittorrent，和ffmpeg处理空间
// 职责 1: 启动时清理残留（你提出的，非常重要！）

// 职责 2: 定期维护（磁盘修复、resume）

// 职责 3: 不再重发消息（让 NATS 处理）
## 4.进程中断导致的数据库表数据不全
直接删除重入队，可放到reconciler中处理